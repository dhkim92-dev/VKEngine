#version 450

layout(local_size_x = 1 , local_size_y = 1, local_size_z = 1) in;
layout(binding=0) buffer TriangleCount{
	uint tri_cnt[];
};
layout(binding=1) buffer CellType{
	uint cell_type[];
};
layout(binding=2) buffer CellCount{
	uint cell_cnt[];
};

void main(){
	
}