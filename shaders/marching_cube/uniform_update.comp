#version 450
layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout (binding=0) buffer EdgeTest{
	uint gin;
};

layout (binding=1) uniform Size{
	uvec4 gout;
};

void main(){
	uint gid = gl_GlobalInvocationID.x * gl_WorkGroupSize.x;
	uint lid = gl_LocalInvocationID.x;
	
	
}